/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    // Enable Turbopack for faster development builds
    turbo: {},
  },
};

module.exports = nextConfig;
